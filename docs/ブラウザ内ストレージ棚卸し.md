# 健康日記 ブラウザ内ストレージ棚卸し

最終更新: 2026-05-06

ソースコード（`js/*.js` 13モジュール / `sw.js` / `index.html`）を一次資料として
直接走査し、ユーザーのブラウザ内に保存される全データを列挙したリファレンス。

姉妹文書: [データ構造.md](データ構造.md)（Store / localStorage / IndexedDB / Firestore / KV の全体図）。

引用は `file:line` 形式で実コードに紐づけ（2026-05-06 時点）。

---

## 0. 全体俯瞰

本アプリは GitHub Pages 上の vanilla JS SPA であり、サーバ側セッションを持たない。
そのためユーザー固有データ・認証トークン・グローバル設定の **すべてがブラウザ内ストレージに集中している**。

| 層 | 用途 | 永続性 | 容量目安 | ソース |
|---|---|---|---|---|
| **localStorage** | 状態スナップショット + 認証/設定/トークン | ブラウザ次第（基本永続） | ~5–10MB | `js/store.js`, 各モジュール直接 |
| **IndexedDB** | 写真 dataURL / オフライン書き込みキュー | 永続 | ~50MB+ | `js/idb.js` |
| **Cache Storage** | 静的アセット（SW） | デプロイで更新 | ~10MB | `sw.js` |
| **sessionStorage** | （未使用） | — | — | — |
| **Cookie** | （未使用、Firebase Auth は IndexedDB 永続化） | — | — | — |

---

## 0-2. 深刻度ランキング（要対応サマリ）

棚卸しの結果、ローカル保存データのうち **「機微度 × 残置/共有のしやすさ × 攻撃容易性」** で
最もリスクが高いと判断したものを 3 段階で抽出した。8 章の構造的観察に対応する。

> [!CAUTION]
> **【Tier 1：致命的】admin 投入の LLM API キー 3 種が全ユーザー共有・平文で残置**
> `apikey_anthropic` / `apikey_openai` / `apikey_google`
> （`firebase-backend.js:445-447` で Firestore から localStorage へ降格）
> - DevTools で誰でも生キーが読める（=即座に課金枠を奪える）
> - `PRESERVE_KEYS`（`store.js:381-383`）に含まれログアウトしても消えない
> - 共用端末では前のユーザーが置いていったキーを後続ユーザーが利用可能
> - 「device-shared」と「per-user」が同じ保存層に同居している設計のため、
>   単純に `clearAll()` すれば直る話でもない

> [!WARNING]
> **【Tier 1：致命的】個人 OAuth トークンがログアウト後も残置**
> `fitbit_token` / `fitbit_refresh_token` / `google_calendar_access_token` / `ics_calendar_url`
> - ログアウトしてもトークンが端末に残るため、共用 PC・端末譲渡時に
>   **次の利用者が前ユーザーの Fitbit / Google Calendar に到達できる**
> - `ics_calendar_url` は秘密 URL の一種であり同様にバウンダリ越え

> [!WARNING]
> **【Tier 1：致命的】医療 PII（要配慮個人情報）が暗号化なしの localStorage 平文**
> `cc_textEntries`（自由記述・音声書き起こし。**PII 含有率最高**）
> `cc_medications` / `cc_bloodTests` / `cc_symptoms` / `cc_vitals` / `cc_userProfile`
> - 個人情報保護法上の「要配慮個人情報」に該当する症状・服薬・検査結果が無暗号
> - 端末紛失・マルウェア・他拡張からの読み取りに対して防御層が無い
> - `Privacy.anonymizeText()` は AI **送信直前**にしか作用せず（`privacy.js:30-`）、
>   かつ `privacy_anonymize_ai` は **既定 OFF**

### Tier 2（深刻）

| データ | キー | 主因 |
|---|---|---|
| 認証 PII | `cc_user`（email / displayName / photoURL） | 平文保存。`cc_isAuthenticated` と整合性ずれ時に幽霊セッション化 |
| AI 送信時の匿名化 | `privacy_anonymize_ai` | 既定 OFF。同意なしで生 PII が外部 LLM へ送られる導線 |
| 監査・課金ログ | `cc_apiUsage`（max 5000件）/ `cc_applicationLog` | 肥大時に `QuotaExceededError` で Store 全体の保存が止まる構造 |

### Tier 3（構造的負債・即危険ではないが要追跡）

- `cc_latestAnalysis` と `cc_analysisHistory[0]` の重複（乖離した時の真値が不明）
- `largeData` オブジェクトストアが未使用（`idb.js`）→ 削除候補
- `cc_*` 命名が「Store 経由」と「Store 外（`cc_language` `cc_errorLog` `cc_affiliate_tracking`）」で混在し判別不能

---

## 1. localStorage（A）— Store クラス経由の `cc_*` キー

`js/store.js:132-155` の `PERSIST_KEYS` に列挙された **44 フィールド**（+ `cc_schema_version`）が
`cc_{フィールド名}` というキーで自動同期される。

- 書き込み: `set()` / `update()` 内の `saveToStorage()`（`store.js:160`）
- 読み込み: 起動時 `loadFromStorage()`（`store.js:177-195`）
- 容量超過時: `QuotaExceededError` を捕捉してトースト表示（`store.js:162-166`）

### 1-A. 認証・UI 状態

| キー | 型 | 意味 |
|---|---|---|
| `cc_user` | object\|null | Firebase Auth ユーザー（uid/email/displayName/photoURL）。<span style="color:#c00">**⚠️ PII あり（平文）**</span> |
| `cc_isAuthenticated` | boolean | ログイン状態フラグ |
| `cc_theme` | string | 固定 `'light'`（CLAUDE.md でダーク禁止） |
| `cc_selectedDisease` | string\|null | 選択中疾患 ID（旧形式） |
| `cc_selectedDiseases` | array | 複数疾患 ID（新形式） |
| `cc_customDiseaseName` | string | カスタム疾患名（"その他" 選択時の自由記述） |
| `cc_selectedModel` | string | AI モデル（既定 `claude-opus-4-6`） |
| `cc_customPrompts` | object | ユーザー上書きプロンプト |
| `cc_dashboardLayout` | string | ダッシュボード表示パターン |

### 1-B. 健康記録（カテゴリ別配列・**PII の本体**）

すべて `[{id, timestamp, ...}]` 形式。記録〜AI 分析の入力となる **アプリの本質的データ**。

| キー | 内容 |
|---|---|
| `cc_symptoms` | 症状（疲労度・痛み等） |
| `cc_vitals` | バイタル（体温・血圧・脈拍） |
| `cc_bloodTests` | 血液検査結果 |
| `cc_medications` | 服薬記録（薬名・用量） |
| `cc_supplements` | サプリメント記録 |
| `cc_meals` | 食事記録 |
| `cc_sleepData` | 睡眠記録 |
| `cc_activityData` | 活動記録 |
| `cc_photos` | 写真 **メタデータ配列のみ**（実 dataURL は IndexedDB） |
| `cc_textEntries` | <span style="color:#c00">**⚠️ フリーテキスト日記（PII 含有率最高、Plaud 由来の音声書き起こしも含む。要配慮個人情報）**</span> |
| `cc_nutritionLog` | 栄養 / BMR ログ |
| `cc_plaudAnalyses` | Plaud 音声分析結果 |
| `cc_calendarEvents` | カレンダー予定（Google/ICS） |

### 1-C. AI 分析・推奨

| キー | 内容 |
|---|---|
| `cc_latestAnalysis` | 直近 1 件 |
| `cc_analysisHistory` | 履歴（max 100 件） |
| `cc_latestFeedback` | 最新フィードバック |
| `cc_latestFeedbackError` | フィードバック失敗時のエラー文字列 |
| `cc_recommendations` | AI 推奨事項 |
| `cc_actionItems` | アクション一覧 |
| `cc_aiComments` | AI コメント履歴 |
| `cc_conversationHistory` | チャット履歴 |
| `cc_deepAnalyses` | 「本格的な分析」アーカイブ |
| `cc_deepAnalysisLastRun` | 1日1回ガード用 JST 日付 |
| `cc_doctorReports` | 医師提出用レポート生成結果 |
| `cc_cachedResearch` | 疾患別研究キャッシュ（Worker→KV→ここ） |
| `cc_cachedActions` | セルフケアパネル（throttle 用） |
| `cc_researchQuery` / `cc_researchDays` / `cc_researchResults` | Research タブの入力＋HTML キャッシュ |
| `cc_healthScore` | 算出済み 0–100 スコア |

### 1-D. メタ・運用

| キー | 内容 |
|---|---|
| `cc_apiUsage` | API 使用ログ（max 5000 件 ≈ 3ヶ月、課金可視化用） |
| `cc_applicationLog` | 監査ログ（金銭操作・管理操作）。`store.js` + `app.js:5434` |
| `cc_globalProfessionals` | 全ユーザー共有の専門家リスト |
| `cc_userProfile` | プロフィール（年齢/性別/身長/体重/言語）。**PII** |
| `cc_integrationSyncs` | Plaud/Fitbit/Google Calendar の最終同期時刻 |
| `cc_affiliateConfig` | アフィリエイトタグ設定 |
| `cc_schema_version` | マイグレーション用バージョン（現行 v2、`store.js:175`） |

---

## 2. localStorage（B）— Store 外の直接アクセス

`grep localStorage\.` で **91 箇所** ヒット。Store 経由を除いた直接アクセスを以下に整理。
`cc_*` 接頭辞で書かれたキーも一部はこちら（Store 外管理）。

### 2-A. システム設定（device-shared）

| キー | 書込 | 意味 |
|---|---|---|
| `firebase_config` | `firebase-backend.js:942` | Firebase 初期化用 JSON。Admin が管理 UI から投入し、以降全ユーザーが共有 |
| `anthropic_proxy_url` | `firebase-backend.js:450` / `:664` / `:755` | Cloudflare Worker プロキシ URL（既定 `stock-screener.agewaller.workers.dev`） |
| `anthropic_mode` | `firebase-backend.js:453` / `:668` | `'direct'` または `'proxy'`。Anthropic 呼び出し経路 |
| `enable_shared_guest_ai` | （UI 経由） | ゲスト共有 AI の ON/OFF（`ai-engine.js:483` で参照） |
| `admin_emails` | `app.js:4875,4890` | 追加管理者メール JSON 配列（ハードコードの `agewaller@gmail.com` は除外） |

### 2-B. <span style="color:#c00">**⚠️ 【最重要】API キー（admin-managed, 全ユーザー共有, 平文）**</span>

```
firebase-backend.js:445-447 / 658-660 / 740-742
```

| キー | 用途 |
|---|---|
| <span style="color:#c00">**`apikey_anthropic`**</span> | <span style="color:#c00">**Claude API。DevTools から直接読み取り可能、ログアウト後も残置**</span> |
| <span style="color:#c00">**`apikey_openai`**</span> | <span style="color:#c00">**OpenAI / GPT-4o。同上**</span> |
| <span style="color:#c00">**`apikey_google`**</span> | <span style="color:#c00">**Gemini / Google Vision。同上**</span> |

設計意図: **Firestore `/admin/config` に管理者が投入したキーを起動時に localStorage へ降格** し、
クライアントから直接 LLM API を叩くか or Worker プロキシ経由で叩く。

> **構造的な含意**
> - DevTools で誰でも生キーが見える。
> - `clearAll()` の `PRESERVE_KEYS`（`store.js:381-383`）に含まれているため、ログアウト後も端末に残る。
> - 共用デバイスでは「前のユーザーが置いていったキー」を後続ユーザーが利用可能。

### 2-C. 個人 OAuth トークン（per-user, per-device）

> [!WARNING]
> 以下の `*_token` 系は **PRESERVE_KEYS によりログアウト後も残置**。
> 共用端末・端末譲渡時に次の利用者が前ユーザーのアカウントへ到達できる。

| キー | 書込 | 意味 |
|---|---|---|
| <span style="color:#c00">**`fitbit_token`**</span> | `integrations.js:265` | <span style="color:#c00">**Fitbit OAuth 2.0 access token（ログアウト残置）**</span> |
| <span style="color:#c00">**`fitbit_refresh_token`**</span> | （`store.js:390` で preserve 対象） | <span style="color:#c00">**Fitbit リフレッシュトークン（ログアウト残置）**</span> |
| `fitbit_client_id` | `app.js:1603` | Fitbit Client ID（公開可） |
| <span style="color:#c00">**`google_calendar_access_token`**</span> | `app.js:1739` | <span style="color:#c00">**Google Calendar OAuth access token（ログアウト残置）**</span> |
| `google_calendar_token_expiry` | `app.js:1740` | 有効期限 epoch |
| `google_calendar_oauth_connected` | `calendar.js:223` | 接続済みフラグ `'1'` |
| <span style="color:#c00">**`ics_calendar_url`**</span> | `calendar.js:96` / `app.js:1843` | <span style="color:#c00">**カレンダー ICS フィード（秘密 URL。漏洩で予定丸見え）**</span> |
| `plaud_email` | （integrations 経由） | Plaud 連携用ユーザー専用受信メールアドレス |
| `apple_health_last_import` | （integrations 経由） | 最終取り込み時刻 |

### 2-D. 個別フラグ・ローカル設定

| キー | 書込 | 意味 |
|---|---|---|
| `cc_language` | `i18n.js:176` | UI 言語（ja/en/zh/ko 等）。**Store 外に独立** |
| `cc_errorLog` | `app.js:21` | グローバル onerror / unhandledrejection ログ（max 50 件） |
| `cc_affiliate_tracking` | `affiliate.js:182` | アフィリクリック/CV ログ（max 1000 件） |
| <span style="color:#c00">**`privacy_anonymize_ai`**</span> | `privacy.js:26` | <span style="color:#c00">**PII 匿名化 ON/OFF。既定 OFF（同意なしで生 PII が外部 LLM へ送信される導線）**</span> |
| `dismissed_inapp_banner` | `inapp-browser-banner.js:149` | アプリ内ブラウザ警告の非表示フラグ |
| `referrer_id` | `firebase-backend.js:346`（読）/ `app.js:111`（書） | 招待元ユーザー ID（UTM 経由、初回ログイン時に消費） |

---

## 3. IndexedDB — `health-diary` DB v1

定義: `js/idb.js:6-7`。サーバー側に置かない大型バイナリと、
Firestore オフライン時の書き込みバッファを保持。

| Object Store | キー | 用途 |
|---|---|---|
| `images` | `id` | 写真 base64 dataURL（`{id, dataUrl, savedAt}`）。localStorage 容量回避 |
| `largeData` | `key` | 予約済み・**現在未使用**（レガシー枠、削除候補） |
| `pendingWrites` | autoIncrement / `collection` index | オフライン時の Firestore 書き込みキュー（復帰時 flush） |

> **構造的観察**: Firestore SDK 自体も `enablePersistence()`（`firebase-backend.js:31`）で
> IndexedDB に独自キャッシュを取る。アプリ独自の `pendingWrites` と SDK のキャッシュは **別物**。

---

## 4. Cache Storage — Service Worker（`sw.js`）

`health-diary-v1-{COMMIT_HASH}` という単一キャッシュに
`index.html` + `js/*.js` 13モジュール + `manifest.json` をプリキャッシュ。
COMMIT_HASH は GitHub Actions で置換されるため、デプロイごとに新キャッシュへ切り替わる。
外部 API（`*.googleapis.com` 等）は対象外（`sw.js:44`）。

---

## 5. データの「重複」と「降格」の構造

健康日記は **同じデータが複数層に複製される設計** が多い：

1. **API キー：Firestore `/admin/config` → localStorage（平文）**
   起動時降格。Firestore Rules で読取制御するはずがクライアントに落とすため、
   端末側に常時平文で存在する。

2. **健康記録：Store(localStorage) ↔ Firestore（ログイン時のみ）**
   Store が真値、Firestore がレプリカ。オフライン時は `pendingWrites` を経由。

3. **写真：localStorage に id 配列のみ + IndexedDB に実体**
   `cc_photos` が IndexedDB レコードを参照。クリーンアップ漏れに注意。

4. **分析履歴：`cc_latestAnalysis` ⊆ `cc_analysisHistory[0]`**
   先頭要素は重複している。

5. **カレンダー：`cc_calendarEvents` + `ics_calendar_url` + `google_calendar_*` + Firestore `/users/{uid}/private/calendar`**
   経路が多い。disconnect 時のクリーンアップ範囲が不明瞭。

---

## 6. PII / 機微情報の所在

| 種別 | 保存層 | キー / フィールド | 暗号化 |
|---|---|---|---|
| 認証情報（メール・氏名・写真URL） | localStorage | `cc_user.{email,displayName,photoURL}` | <span style="color:#c00">**なし**</span> |
| <span style="color:#c00">**医療記録（症状・服薬・血液検査）= 要配慮個人情報**</span> | localStorage | `cc_symptoms` / `cc_medications` / `cc_bloodTests` 他 | <span style="color:#c00">**なし ⚠️**</span> |
| <span style="color:#c00">**自由記述・音声書き起こし（PII 含有率最高）**</span> | localStorage | `cc_textEntries`, `cc_plaudAnalyses` | <span style="color:#c00">**なし ⚠️**</span> |
| プロフィール（年齢・身長・体重） | localStorage | `cc_userProfile` | <span style="color:#c00">**なし**</span> |
| カレンダータイトル・本文 | localStorage | `cc_calendarEvents` | <span style="color:#c00">**なし**</span> |
| 写真の実バイト | IndexedDB | `images` ストア | <span style="color:#c00">**なし**</span> |
| <span style="color:#c00">**OAuth トークン（ログアウト残置）**</span> | localStorage | `google_calendar_access_token`, `fitbit_token` | <span style="color:#c00">**なし ⚠️**</span> |
| <span style="color:#c00">**LLM API キー（全ユーザー共有・残置）**</span> | localStorage | `apikey_*` | <span style="color:#c00">**なし ⚠️**</span> |

`Privacy.anonymizeText()`（`privacy.js:30-`）は AI 送信**直前**のみ作用するため、
ストレージ自体の PII は一切マスクされない。かつ既定 OFF。

---

## 7. ログアウト時の残置（`store.clearAll()` の挙動）

`store.js:373-394` の `PRESERVE_KEYS` に基づき、ログアウト後も以下が残る：

```
firebase_config / anthropic_proxy_url / anthropic_mode / admin_emails /
enable_shared_guest_ai / apikey_anthropic / apikey_openai / apikey_google /
ics_calendar_url / google_calendar_oauth_connected /
fitbit_token / fitbit_client_id / fitbit_refresh_token /
apple_health_last_import / plaud_email
```

加えて `localStorage.clear()` で消えても **IndexedDB は別途保持**
（`images` / `pendingWrites` も残る点に注意）。
コメント上の意図は「再ログイン時の再設定を不要にする」だが、
共用端末・端末譲渡時のシナリオは未考慮。

---

## 8. 構造的観察（改修議論の種）

凡例: <span style="color:#c00">**🔴 致命的**</span> / **🟠 深刻** / 🟡 構造的負債

1. <span style="color:#c00">**🔴 API キーの平文 localStorage 永続化**</span>（`store.js:381-383`, `firebase-backend.js:445-447`）
   → DevTools 露出 + ログアウト後残置 + 共用端末で前ユーザーのキーが再利用される。
2. <span style="color:#c00">**🔴 PII 匿名化が opt-in & 既定 OFF**</span>（`privacy.js:9-10`）
   → 匿名化対象もストレージ自体ではなく送信直前のみ。ストレージ側の暗号化は無し。
3. 🟡 **`largeData` オブジェクトストアが未使用**（`idb.js`）
   → レガシー枠。削除可能か確認要。
4. 🟡 **`cc_latestAnalysis` と `cc_analysisHistory[0]` の重複**
   → 整合性ロジックが Store にあるか、ある時点で乖離するか要検証。
5. **🟠「device-shared API キー」と「per-user OAuth トークン」が同一 PRESERVE_KEYS に同居**
   → 設計上の役割分離が崩れている。clearAll() で per-user 側も消したくなるが、
   　device-shared 側の再設定コストが障壁になっている構造。
6. **🟠 IndexedDB `pendingWrites` の retry / 失敗時挙動**
   → 詳細追加調査が必要。失敗が永続化されるとオフライン時の編集が静かに消える可能性。
7. 🟡 **`cc_*` 命名の混在**（Store 経由は `cc_{PERSIST_KEYS の名}`、別系統は `cc_language` `cc_errorLog` `cc_affiliate_tracking`）
   → 一目で「Store 管理」か否か判定できない。

---

## 9. 検証方法

ブラウザの DevTools で以下を開けば本書の各キーが確認できる：

- `Application` → `Storage` → `Local Storage` → `https://cares.advisers.jp`
- `Application` → `Storage` → `IndexedDB` → `health-diary` → `images` / `largeData` / `pendingWrites`
- `Application` → `Storage` → `Cache Storage` → `health-diary-v1-*`

ローカル開発時:

```
npm start
# → http://localhost:8080 で同様に確認
```

機械的な再列挙には:

```
grep -rn "localStorage\." js/ index.html
grep -A2 "PERSIST_KEYS" js/store.js
grep -A2 "PRESERVE_KEYS" js/store.js
```